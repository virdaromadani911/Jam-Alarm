import 'package:flutter/material.dart';

class ClockWidget extends StatelessWidget {
  // Tambahkan properti atau parameter sesuai kebutuhan
  @override
  Widget build(BuildContext context) {
    // Koding untuk tampilan jam
    return Container();
  }
}
